
var currentAge = 23;
var maxAge = 80;
var amountDay = 3;
var totalAmount = (maxAge - currentAge) * amountDay;

console.log('You will need ' + totalAmount +
' to last you until the ripe gold age of ' + maxAge);